#include<stdio.h>
#include<string.h>
int main(void){
    switch (/*History Options*/){
        case a;//Default mode - Add website to history 
        scanf(/*website name*/);
        Add();
        case d;
        scanf(/*Website Line Number*/);//Line number of the website the user wants to delete
        Delete();
        case h;//Open up the user's website history
        History();
    }
}    
void Add(){
//For every website visited, add the website to the top of the list. 
}
void Delete(int){
//Remove the website the user wants to delete and move the websites below the deleted website up.
}
void History(){
//User press the key "H" to view their entire website history.
}

